import 'dart:typed_data';

Future<bool> downloadBytes(Uint8List bytes, String filename, String mime) async => false;
String? bytesToBlobUrl(Uint8List bytes, String mime) => null;
void revokeBlobUrl(String url) {}
